<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lilac.mapper.UserMapper">
    <insert id="save">
        insert into user (name, card_id, age,gender,is_member,phone,address,create_time,update_time)
        values (#{name},#{cardId},#{age},#{gender},#{isMember},#{phone},#{address},#{createTime},#{updateTime})
    </insert>

    <update id="update">
        update user set name=#{name},card_id=#{cardId}, age=#{age}, gender=#{gender}, is_member= #{isMember},phone=#{phone},address=#{address}, update_time=#{updateTime}
        where id=#{id}
    </update>

    <select id="pageList" resultType="com.lilac.entity.User">
        select * from user
        <where>
            <if test="name != null">
                name like concat('%', #{name}, '%')
            </if>
            <if test="phone != null">
                and phone like concat('%', #{phone}, '%')
            </if>
        </where>
    </select>

    <select id="selectByPhone" resultType="com.lilac.entity.User">
        select id, phone from user
        where phone = #{phone}
        <if test="excludeId != null">
            and id != #{excludeId}
        </if>
        limit 1
    </select>
    <select id="existsByCardId" resultType="java.lang.Integer">
        select 1 from user where card_id = #{cardId} limit 1
    </select>
</mapper>