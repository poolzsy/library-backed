<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lilac.mapper.AdminMapper">
    <insert id="save">
        insert into admin (user_name,password,phone, email,create_time,update_time)
        values (#{userName},#{password},#{phone},#{email},#{createTime},#{updateTime})
    </insert>

    <update id="update">
        update admin set user_name=#{userName},phone=#{phone},email=#{email}, update_time=#{updateTime}
        where id=#{id}
    </update>

    <select id="pageList" resultType="com.lilac.entity.Admin">
        select * from admin
        <where>
            <if test="userName != null">
                name_name like concat('%', #{userName}, '%')
            </if>
            <if test="phone != null">
                and phone like concat('%', #{phone}, '%')
            </if>
            <if test="email != null">
                and email like concat('%', #{email}, '%')
            </if>
        </where>
    </select>
</mapper>